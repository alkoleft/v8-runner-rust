# Example configuration for v8-test-runner
# Copy to application.yaml and adjust paths for your project

# Root path of the project sources (Designer format)
basePath: /path/to/project/sources

# Working directory for temp files and hash storages
workPath: /tmp/v8-test-runner/my-project

# Source format: DESIGNER or EDT
format: DESIGNER

# Builder backend: DESIGNER or IBCMD
builder: DESIGNER

# 1C infobase connection string
connection: "File=/path/to/infobase"

# Source sets: list of configurations and extensions
source-set:
  - name: main
    purpose: CONFIGURATION
    path: .  # relative to basePath

  # Uncomment to add an extension:
  # - name: my-extension
  #   purpose: EXTENSION
  #   path: extensions/my-extension

# Platform tools configuration (optional)
tools:
  platform:
    # Installation hint can point either to a binary:
    # path: /opt/1cv8/x86_64/8.3.25.1234/1cv8
    #
    # ...or to an installation/bin directory:
    # path: /opt/1cv8/x86_64
    #
    # Exact platform version for auto-discovery:
    version: "8.3.25.1234"

  edt-cli:
    # path: /opt/1C/1CE/components/1c-edt-2025.1.0+656-x86_64/1cedt/1cedtcli
    auto-start: false
