# SignCfg

## Назначение

Подписывает расширение конфигурации цифровой подписью. После выполнения процедуры подписи расширение может быть использовано в базовых версиях конфигураций, подписанных тем же ключом, что и расширение. Допустимо указывать следующие каталоги и использовать следующие параметры:.

## Синтаксис

```text
/SignCfg -SignedFile <путь к cfe-файлу> -ConfigurationType <тип конфигурации> [-Name <имя расширения>]
[-Version <версия>] [-File <путь к cfe-файлу>] -digisign <имя файла с параметрами лицензирования>
```

## Примечания

- Допустимо указывать следующие каталоги и использовать следующие параметры: `-SignedFile` <путь к cfe`-файлу`> ‑ путь к результирующему файлу.
